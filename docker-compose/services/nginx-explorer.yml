version: '3.9'

services:
  proxy:
    image: nginx
    container_name: proxy
    environment:
      BACK_PROXY_PASS: ${BACK_PROXY_PASS:-http://127.0.0.1:4000}
      FRONT_PROXY_PASS: ${FRONT_PROXY_PASS:-http://127.0.0.1:3000}
    ports:
      - target: 80
        published: 8080
